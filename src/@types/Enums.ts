export enum TaskStatus {
  TODO = "TODO",
  IN_PROGRESS = "IN_PROGRESS",
  REVIEW = "REVIEW",
  RELEASE = "RELEASE",
  DONE = "DONE",
}

export enum UserRole {
  Admin = "Admin",
  Manager = "Manager",
  User = "User",
}

export enum ProjectStatus {
  NOT_STARTED = 0,
  IN_PROGRESS = 1,
  COMPLETED = 2,
  CANCELLED = 3,
}
