export enum TaskStatus {
  TODO = "TODO",
  IN_PROGRESS = "IN_PROGRESS",
  REVIEW = "REVIEW",
  RELEASE = "RELEASE",
  DONE = "DONE",
}

export enum UserRole {
  Admin = "Admin",
  Manager = "Manager",
  User = "User",
}
