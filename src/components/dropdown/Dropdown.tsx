import DropdownProps from "./DropdownProps";

const Dropdown = ({ children, className }: DropdownProps) => {
  return (
    <div className={`${className} p-4 bg-green-300 w-fit`}>
      {children}

      render item()
    </div>
  );
};

export default Dropdown;
